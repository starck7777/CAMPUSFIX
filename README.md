# CAMPUSFIX
CampusFix is a Python + Flask-based campus issue tracking app that lets users report and monitor issues with status updates. It features dashboards, filters, and admin tools for SQLite backup/export. It runs locally, supports portable Windows builds, and includes a simple web UI.
